# Minimal runtime dependencies (no WeasyPrint; uses simple ReportLab PDF fallback)
# Recommended for Windows if you don't want to install C++ build tools.
# Works with Python 3.11 or 3.12 (avoid very new major versions until wheels are published).
pydantic>=2.5
python-dotenv>=1.0
httpx>=0.27
jinja2>=3.1
reportlab>=4.0
openpyxl>=3.1
typer>=0.12  # removed [all] extra (rich installed separately)
rich>=13.7
python-gitlab>=4.4
schedule>=1.2
fastapi>=0.111
uvicorn>=0.30
pluggy>=1.5
diskcache>=5.6
apscheduler>=3.10
python-multipart>=0.0.9
pytest>=8.0
